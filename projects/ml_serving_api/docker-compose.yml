version: "3.8"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
      - SECRET_KEY=your-secret-key-here
      - MODEL_PATH=/app/models
    volumes:
      - ./app:/app/app
    restart: unless-stopped
